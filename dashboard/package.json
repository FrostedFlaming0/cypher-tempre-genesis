{
  "name": "cypher-tempre-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first Cypher Tempre Timechain dashboard with Base ERC-20 token-gated access.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "bridge": "node server.mjs",
    "build:walletconnect": "esbuild src/reown-wallet.js --bundle --format=esm --target=es2020 --outfile=public/reown-wallet.js",
    "check": "node --check server.mjs && node --check public/app.js && node --check src/reown-wallet.js && node --check public/reown-wallet.js"
  },
  "dependencies": {
    "@reown/appkit": "^1.8.20",
    "@reown/appkit-adapter-ethers": "^1.8.20",
    "esbuild": "^0.28.0",
    "ethers": "6.16.0"
  },
  "overrides": {
    "ws": "8.20.1"
  },
  "engines": {
    "node": ">=20"
  }
}
